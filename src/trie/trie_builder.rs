use crate::Trie;

pub struct TrieBuilder {}

impl TrieBuilder {
    pub fn new() -> Self {
        TrieBuilder {}
    }

    // TODO generics
    pub fn push(&mut self, word: &str) {}

    pub fn build(&self) -> Trie {
        Trie {}
    }
}
